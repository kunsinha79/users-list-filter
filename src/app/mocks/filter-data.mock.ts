import { FilterData } from '../interfaces/filter-data.interface';

export const filterData: FilterData = {filter: 'Bell', filterField: 'name'};

export const filterNoData: FilterData = {filter: 'something', filterField: 'name'};